import React from 'react';
import Header from '../../Shared/Header/Header';

const Dashboard = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default Dashboard;